// Seed data consolidated into seed.ts — run that instead.
// npx ts-node prisma/seed.ts
async function main() {
  console.log('ℹ️  All seed data is now in prisma/seed.ts. Run that script instead.');
}
main().catch(console.error);
